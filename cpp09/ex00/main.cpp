#include <iostream>
#include <fstream>
#include <string>

int main (int argc, char *argv) {
    //argv를 strng으로 만들어서 ifstream으로 넣음!;
    ifstream file("");
    //file을 argument로 받아야함.
    // file은  "date | value"의 포맷을 지켜야함
    // valid date => Year-Month-Day.
    // valid value => float or a positive integer between 0  ~ 1000;
    // 최소한 하나의 컨테이너를 써라~~
    // 적절한 에러 메세지로 에러를 핸들링해라~~

}